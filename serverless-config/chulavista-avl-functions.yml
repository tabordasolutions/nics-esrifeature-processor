montevista-units-avl:
  handler: lambda.handler
  events: ${file(${self:provider.envVarsFile}):montevista_units_avl.events}
  description: "ETL Monte Vista Units AVL data provided by ChulaVista ESRI service. The data is stored in SCOUT database."
  memorySize: 256
  timeout: 4
  environment: ${file(${self:provider.envVarsFile}):montevista_units_avl.environment}